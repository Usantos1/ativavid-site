# ativavid.com

Página de vendas do ATIVAVID — estática (HTML/CSS/JS), sem build.

- `index.html`, `styles.css`, `main.js`
- `assets/` — logo, telas reais do app, imagem OG

Publicada pela Cloudflare Pages a partir do branch `main`.
